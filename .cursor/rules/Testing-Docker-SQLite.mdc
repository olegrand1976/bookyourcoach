---
description: Tests Laravel via runner Docker php-test (SQLite)
globs: tests/**/*.php,phpunit.xml,docker-compose*.yml,docker/php-test/**
alwaysApply: false
---

# Tests Docker SQLite

Préférer `composer test:docker` ou `composer test:docker:local` (service `php-test`, profil `test`).

Si erreur `could not find driver` (sqlite) en local → relancer via `php-test`, pas le PHP hôte.
