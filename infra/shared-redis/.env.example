# Mot de passe fort — identique côté chaque application (REDIS_PASSWORD)
REDIS_PASSWORD=change_me_strong_password

# Allocation REDIS_DB / REDIS_CACHE_DB par application (côté .env de chaque app) :
#   bookyourcoach : 0 / 1
#   autre-app     : 2 / 3
# Isolation complémentaire via REDIS_PREFIX (slug APP_NAME Laravel)
